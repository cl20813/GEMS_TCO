# Data acquisition scripts

This directory contains project-specific data download utilities that are not
part of the statistical package. The downloader reads its API credential from
the `GEMS_TCO_API_KEY` environment variable (or an explicit constructor
argument); credentials must not be committed to the repository.
